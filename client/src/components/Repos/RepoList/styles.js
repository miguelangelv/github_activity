import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    root: {
        width: '100%',
        marginTop: '20px',
        marginBottom: '9px',
    },
    container: {
        maxHeight: '340px',
    },
    tableRow: {
        cursor: 'pointer',
    }
}));
